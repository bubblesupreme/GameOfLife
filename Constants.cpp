#include "Constants.h"


User user;
const int sizeBooth = 20;
const Size sizeGrid(40, 30);
const Size sizeGridLevel(30, 30);
const float bandWidth = 58.0;
Resources* resources = new Resources();
const LevelBlanks levelBlanks;
std::string fileName("users.txt");
sf::Sound sound;